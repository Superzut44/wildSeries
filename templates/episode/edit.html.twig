{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'Edit a episode'|trans }}</h1>

    {{ include('episode/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('episode_index') }}">{{ 'Back to list'|trans }}</a>

    {{ include('episode/_delete_form.html.twig') }}
{% endblock %}
