{% extends 'base.html.twig' %}

{% block body %}

<h1>{{ 'Categories'|trans }} :</h1>

<ul>
    {% for category in categories %}
        <li>
            <a href="{{ path("category_show", { categoryName: category.name }) }}">{{ category.name }}</a>
        </li>
    {% endfor %}
</ul>

    <a href="{{ path('category_new') }}">{{ 'Create new category'|trans }}</a>
{% endblock %}