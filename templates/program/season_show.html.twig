{% extends "base.html.twig" %}

{% block title %}Saison {{ season.number }}{% endblock %}

{% block body %}
<div class="media">
    <h1>Saison {{ season.number }} de {{ program.title }}</a></h1>
    <p>Liste des épisodes</p>
    <ul class="program_season_show">
    {% for episode in season.episodes %}
            <li>
                <a href="{{ path('program_episode_show', {
                    program: program.id , season: season.id , episode: episode.id }) }}"
                    >Episode {{ episode.number }} : {{ episode.title }}</a>
            </li>
    {% endfor %}
    </ul>
</div>
{% endblock %}
