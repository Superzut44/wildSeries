{% extends "base.html.twig" %}

{% block body %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save')|trans }}</button>
    {{ form_end(form) }}

    <a href="{{ path('program_index') }}">{{ 'Back to the list'|trans }}</a>
{% endblock %}
